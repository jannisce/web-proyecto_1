import { render } from '@testing-library/react'
import { describe, it } from 'vitest'

import RiotForge from './RiotForge'

describe('RiotForge component', () => {
  it('Renders correctly :)', () => {
    render(RiotForge)
  })
})